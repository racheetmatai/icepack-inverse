# Icepack source overrides

These files are exact copies of the locally modified modules used from the
surrounding Icepack checkout at commit
`28eed36fe652da79769d0130822037f903b23ed3`.

They are preserved here because the surrounding checkout points to the
upstream `icepack/icepack` repository and is not the project repository.
During later streamlining, convert these copies into a documented patch or a
small project-local compatibility layer only after their role has been tested.
